"use client"

import { useI18n } from "@/lib/i18n"

export function TestimonialsSection() {
  return null
  /*
  const { t, locale } = useI18n()

  return (
    <section id="testimonials" className="border-t border-border bg-secondary/30 py-20 lg:py-28">
      <div className="mx-auto max-w-7xl px-6">
        <div className="mx-auto mb-16 max-w-3xl text-center">
          <h2 className="text-balance font-display text-3xl font-bold text-foreground md:text-4xl">
            {t("testimonials.title")}
          </h2>
          <p className="mt-4 text-pretty text-lg leading-relaxed text-muted-foreground">
            {t("testimonials.subtitle")}
          </p>
        </div>
        <div className="mx-auto max-w-2xl rounded-2xl border border-dashed border-border bg-background/50 p-8 text-center">
          <div className="mb-4 flex justify-center">
            <span className="rounded-full bg-primary/10 px-3 py-1 text-xs font-medium text-primary">
              Coming Soon
            </span>
          </div>
          <p className="text-sm leading-relaxed text-muted-foreground">
            {locale === "zh"
              ? "我们正在收集真实用户反馈，稍后将展示经过审核的评价内容。"
              : "We are collecting verified user feedback and will publish approved testimonials soon."}
          </p>
        </div>
      </div>
    </section>
  )
  */
}
